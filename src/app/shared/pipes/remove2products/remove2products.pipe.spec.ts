import { Remove2productsPipe } from './remove2products.pipe';

describe('Remove2productsPipe', () => {
  it('create an instance', () => {
    const pipe = new Remove2productsPipe();
    expect(pipe).toBeTruthy();
  });
});
